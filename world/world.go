package world

// MaxRenderRadius is the maximum value for the render radius of chunks. It is
// the maximum distance a player could see ahead of them (measured in chunks).
const MaxRenderRadius = 32
